---
const stats = [
  { value: "₹4,000+ Cr", label: "Completed Projects" },
  { value: "₹1,000 Cr", label: "Ongoing Projects" },
  { value: "21+ States", label: "Pan-India Presence" },
  { value: "10+ Years", label: "MoFPI & Cluster Expertise" },
  { value: "Global Reach", label: "Offices in Japan, USA, Africa" },
];
---

<section id="about" class="min-h-[100vh] flex items-center py-20 bg-white sm:py-28 font-[Poppins,sans-serif]">
  <div class="max-w-7xl px-8 mx-auto sm:px-10 lg:px-16">
    <!-- Main grid container with content on the left and image on the right -->
    <div class="grid items-center gap-16 md:grid-cols-5">

      <!-- Left (Content section - Now smaller with md:col-span-2) -->
      <div class="relative md:col-span-2">
        <h2 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
          Who We Are
        </h2>
        <div class="w-24 h-1 mt-4 bg-yellow-500 rounded"></div>
        
        <p class="mt-6 text-gray-700 text-s leading-relaxed tracking-wide">
          Established in <strong>November 2011</strong>, 
          <span class="font-bold">Abhyuday</span> is a premier Project Management Consultancy (PMC) with over a decade of driving excellence in the development and implementation of transformative projects across the <strong>Food Processing, Textile and Defence sectors.</strong>
        </p>
        
        <p class="mt-6 text-s font-medium text-yellow-600 uppercase tracking-wide leading-relaxed">
          With a nationwide reach across <strong>21+ Indian states</strong>, including the North-East, and a global presence with offices in <strong>Japan, USA, and Africa</strong>, our commitment to excellence is reflected in over <strong>₹4,000 Cr</strong> of completed projects and <strong>₹1,000 Cr</strong> in ongoing initiatives.
        </p>
      </div>

      <!-- Right (Image section - Now larger with md:col-span-3) -->
      <div class="relative md:col-span-3">
        <!-- Decorative Circles - Adjusted to be on the top-right side of the image -->
        <div class="absolute top-[-40px] right-[-40px] w-28 h-28 border-2 rounded-full border-blue-400 z-0"></div>
        <div class="absolute top-[-20px] right-0 w-36 h-36 border-2 rounded-full border-blue-200 z-0"></div>

        <!-- Image -->
        <div class="relative z-10 h-[400px] w-full rounded-xl shadow-lg overflow-hidden bg-[url('/AboutUs.png')] bg-cover bg-center"></div>

        <!-- Since 2011 Box - Adjusted to be on the bottom-right side of the image -->
        <div class="absolute px-6 py-3 text-sm font-semibold text-blue-600 bg-white rounded-xl shadow-xl bottom-10 right-[-40px] z-20 sm:text-base">
          Since <span class="sm:text-2xl">&nbsp;2011</span>
        </div>
      </div>
    </div>

    <!-- Stats, now a separate section below the main grid -->
    <!-- The grid-cols-4 class ensures all stats appear in one row on large screens -->
    <div class="w-full mt-16 grid grid-cols-2 lg:grid-cols-5 gap-6">
      {
        stats.map((stat) => (
          <div class="bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 p-8 text-center">
            <h3 class="text-2xl font-bold text-gray-900">{stat.value}</h3>
            <p class="mt-2 text-medium text-gray-600">{stat.label}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
